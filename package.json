{
  "name": "tweet-button-webext",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "mocha --ui tdd",
    "format": "prettier --write .",
    "format-check": "prettier --check ."
  },
  "devDependencies": {
    "mocha": "^8.3.2",
    "prettier": "^2.2.1",
    "twitter-text": "^3.1.0"
  }
}
