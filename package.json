{
  "name": "tweet-button-webext",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "./scripts/build.sh",
    "test": "node --test",
    "format": "prettier --write .",
    "format-check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.3.2",
    "twitter-text": "^3.1.0"
  }
}
