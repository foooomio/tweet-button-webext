{
  "name": "tweet-button-webext",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "mocha --ui tdd",
    "format": "prettier --write .",
    "format-check": "prettier --check ."
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "prettier": "^2.6.2",
    "twitter-text": "^3.1.0"
  }
}
